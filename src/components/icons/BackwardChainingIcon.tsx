import Icon from "./svg/backward-chaining.svg?react";

/** Backward chaining — the same diagonal chain-link glyph as
 *  ForwardChainingIcon, but with the arrow entering from the bottom-right,
 *  pointing back up into the link's trailing end: teaching starts at the
 *  last step of the chain and moves backward toward the first. Source:
 *  ./svg/backward-chaining.svg (edit there — this file just re-exports it as
 *  a component). */
export const BackwardChainingIcon = Icon;
