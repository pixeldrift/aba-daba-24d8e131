import Icon from "./svg/approve-edit.svg?react";

/** Same pencil glyph as RequestEditIcon, with a checkmark tucked into the
 *  same tip-corner spot the "?" occupies there — marks an edit as
 *  APPROVED (the request went through) rather than pending.
 *  Source: ./svg/approve-edit.svg. */
export const ApproveEditIcon = Icon;
