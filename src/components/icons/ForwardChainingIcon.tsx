import Icon from "./svg/forward-chaining.svg?react";

/** Forward chaining — a diagonal chain link (rotated 45°, same "link" glyph
 *  used for the synced-timer icon) with an arrow entering from the top-left,
 *  pointing down into the link's leading end: teaching starts at the first
 *  step of the chain and moves forward through it. Pairs with
 *  BackwardChainingIcon, whose arrow instead enters from the bottom-right.
 *  Source: ./svg/forward-chaining.svg (edit there — this file just re-exports
 *  it as a component). */
export const ForwardChainingIcon = Icon;
