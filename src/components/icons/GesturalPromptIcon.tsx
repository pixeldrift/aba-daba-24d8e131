import Icon from "./svg/gestural-prompt.svg?react";

/** Gestural prompt — a minimal pointing hand (index finger extended,
 *  others curled). Source: ./svg/gestural-prompt.svg (edit there — this
 *  file just re-exports it). */
export const GesturalPromptIcon = Icon;
