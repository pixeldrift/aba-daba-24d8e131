/// <reference types="vite/client" />
/// <reference types="vite-plugin-svgr/client" />

/** Short git commit hash, injected by vite.config.ts's `define` — see its
 *  own comment for why. */
declare const __APP_VERSION__: string;
